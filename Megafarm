local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local RunService = game:GetService("RunService")
local VIM = game:GetService("VirtualInputManager")
local LP = Players.LocalPlayer

-- Безопасно находим папку Particles
local ParticlesFolder = workspace:WaitForChild("Particles", 10)

local IsRunning = true
local AFK_POS = Vector3.new(287, -40, -16)
local Blacklist = {}
local PermanentBlacklist = {} 

if CoreGui:FindFirstChild("MegaFarmGui") then CoreGui.MegaFarmGui:Destroy() end
local ScreenGui = Instance.new("ScreenGui", CoreGui)
ScreenGui.Name = "MegaFarmGui"

local Frame = Instance.new("Frame", ScreenGui)
Frame.Size = UDim2.new(0, 130, 0, 40)
Frame.Position = UDim2.new(0.5, -65, 0, 20)
Frame.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
Frame.BorderSizePixel = 0

local Title = Instance.new("TextLabel", Frame)
Title.Size = UDim2.new(0.75, 0, 1, 0)
Title.Text = "MEGA FARM"
Title.TextColor3 = Color3.new(1, 1, 1)
Title.BackgroundTransparency = 1
Title.Font = Enum.Font.SourceSansBold
Title.TextSize = 14

local CloseBtn = Instance.new("TextButton", Frame)
CloseBtn.Size = UDim2.new(0.25, 0, 1, 0)
CloseBtn.Position = UDim2.new(0.75, 0, 0, 0)
CloseBtn.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.new(1, 1, 1)
CloseBtn.Font = Enum.Font.SourceSansBold

print("[MEGA FARM] Скрипт запущен. Проверка TouchTransmitter активна.")

local function getRoot()
    return LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
end

local function getHumanoid()
    return LP.Character and LP.Character:FindFirstChild("Humanoid")
end

local function stopFarming()
    IsRunning = false
    print("[MEGA FARM] Фарм остановлен.")
    ScreenGui:Destroy()
end

CloseBtn.MouseButton1Click:Connect(stopFarming)

-- Новая точная проверка по твоим скриншотам
local function isValidCashPart(part)
    if not part or not part:IsA("BasePart") or part.Name ~= "Cash" or not part.Parent then
        return false
    end
    
    -- Ищем внутри парта объект, у которого класс равен "TouchTransmitter" 
    -- или имя которого "TouchInterest"
    for _, child in pairs(part:GetChildren()) do
        if child.ClassName == "TouchTransmitter" or child.Name == "TouchInterest" then
            return true
        end
    end
    
    return false
end

-- Функция собирает только BasePart с именем "Cash" и TouchTransmitter внутри
local function getValidCash()
    local validItems = {}
    if not ParticlesFolder then return validItems end
    
    for _, v in pairs(ParticlesFolder:GetChildren()) do
        if not Blacklist[v] and not PermanentBlacklist[v] then 
            if isValidCashPart(v) then
                table.insert(validItems, v) 
            end
        end
    end
    return validItems
end

local function onCharacterAdded(char)
    if not IsRunning then return end
    local root = char:WaitForChild("HumanoidRootPart", 5)
    if root then
        local cashItems = getValidCash()
        if #cashItems == 0 then
            root.Velocity = Vector3.new(0, 0, 0)
            root.CFrame = CFrame.new(AFK_POS)
        end
    end
end

if LP.Character then task.spawn(onCharacterAdded, LP.Character) end
LP.CharacterAdded:Connect(onCharacterAdded)

-- Защита от AFK-кика
task.spawn(function()
    while IsRunning do
        local hum = getHumanoid()
        if hum and hum.Health > 0 then
            VIM:SendKeyEvent(true, Enum.KeyCode.Space, false, game)
            task.wait(0.05)
            VIM:SendKeyEvent(false, Enum.KeyCode.Space, false, game)
        end
        task.wait(1)
    end
end)

local function collect(item)
    if not IsRunning or Blacklist[item] or PermanentBlacklist[item] then return end
    if not isValidCashPart(item) then return end

    local root = getRoot()
    local hum = getHumanoid()
    
    while (not root or not hum) and IsRunning do
        task.wait(0.1)
        root = getRoot()
        hum = getHumanoid()
    end
    
    if not root or not item.Parent then return end

    print("[MEGA FARM] Телепорт на парт:", item:GetDebugId(4))
    local counter = 0
    local startTime = tick()

    repeat
        if not IsRunning then break end
        
        if hum and hum.Health <= 0 then
            warn("[MEGA FARM] Смерть при сборе. Блеклист:", item:GetDebugId(4))
            PermanentBlacklist[item] = true 
            break
        end

        if tick() - startTime > 3 then
            warn("[MEGA FARM] Зависание сбора (3 сек). Пропуск:", item:GetDebugId(4))
            Blacklist[item] = true
            task.delay(10, function()
                Blacklist[item] = nil
            end)
            break
        end

        root = getRoot()
        hum = getHumanoid()

        if root and hum and item.Parent and isValidCashPart(item) then
            local offset = math.sin(counter) * 5
            local rotX = math.cos(counter) * 180
            local rotY = counter * 150
            local rotZ = math.sin(counter) * 180
            
            root.CFrame = item.CFrame * CFrame.new(offset, 0, 0) * CFrame.Angles(math.rad(rotX), math.rad(rotY), math.rad(rotZ))
            counter = counter + 0.3
        end
        task.wait()
    -- Цикл завершается мгновенно, как только TouchTransmitter исчезает из парта
    until not item or not item.Parent or not isValidCashPart(item) or not item:IsDescendantOf(workspace) or not IsRunning or Blacklist[item] or PermanentBlacklist[item]
end

-- Основной цикл сканирования
task.spawn(function()
    local lastCount = -1
    while IsRunning do
        local root = getRoot()
        local hum = getHumanoid()
        
        if root and hum and hum.Health > 0 then
            local cashItems = getValidCash()

            if #cashItems > 0 then
                if #cashItems ~= lastCount then
                    print("[MEGA FARM] Найдено реальных Cash с TouchTransmitter:", #cashItems)
                    lastCount = #cashItems
                end
                
                for _, item in pairs(cashItems) do
                    if not IsRunning or Blacklist[item] or PermanentBlacklist[item] then break end
                    collect(item)
                end
            else
                lastCount = 0
            end
        end
        task.wait(0.2)
    end
end)

-- Удержание AFK
local isAlreadyAFK = false
RunService.Heartbeat:Connect(function()
    if not IsRunning then return end
    
    local root = getRoot()
    local hum = getHumanoid()
    
    if root and hum and hum.Health > 0 then
        local cashItems = getValidCash()

        if #cashItems == 0 then
            if not isAlreadyAFK then
                print("[MEGA FARM] Партов Cash с TouchTransmitter нет. Ожидание на AFK позиции.")
                isAlreadyAFK = true
            end
            root.Velocity = Vector3.new(0, 0, 0)
            root.RotVelocity = Vector3.new(0, 0, 0)
            root.CFrame = CFrame.new(AFK_POS)
        else
            isAlreadyAFK = false
        end
    end
end)

-- Моментальный перехват новых купюр при спавне
if ParticlesFolder then
    ParticlesFolder.ChildAdded:Connect(function(child)
        if IsRunning then
            task.wait(0.05)
            if isValidCashPart(child) then
                print("[MEGA FARM] Спавн нового Cash с TouchTransmitter:", child:GetDebugId(4))
                collect(child)
            end
        end
    end)
end
